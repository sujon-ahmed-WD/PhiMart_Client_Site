import { useEffect, useState } from "react";
import apiClient from "../services/api-client";
import { data } from "react-router";

const useAuth = () => {
    const [user, setUser] = useState(null);

    const getToken = () => {
        const token = localStorage.getItem("authTokens");
        return token ? JSON.parse(token) : null;
    };
    const [authTokens, setAuthTokens] = useState(getToken());

    useEffect(()=>{ // ae function kno use korsa bhi 
       if(authTokens) fetchUserProfile();

    },[authTokens]); 

    // Fetch user Profile
    const fetchUserProfile=async()=>{ 
        
        try{

            const response=await apiClient.get("auth/users/me/",{
                headers:{Authorization: `JWT ${authTokens?.access}`},
            });
            if (!response){
                console.log(response);
            }
                
            
        } 

        catch(error)
        {
            console.log("Error Fletching  user",error)
        }
    }


    // Login user
    const loginUser = async (userData) => {
        try{
            const response = await apiClient.post("auth/jwt/create/", userData);
            setAuthTokens(response.data)
            localStorage.setItem("authTokens",JSON.stringify(response.data))

        } 
        catch(error)
        {
            console.log("Login Error",error?.data.response)
        }

    }

    return { user, loginUser }
};
export default useAuth;

//        } 
        
        catch(error)
        {
            console.log("Error Fletching  user",error)
        }
    }

    amier akna password jodi rong dai tah hola 
    POST http://127.0.0.1:8000/api/v1/auth/jwt/create/ 401 (Unauthorized)
    useAuth.js:50 Uncaught (in promise) TypeError: Cannot read properties of undefined (reading 'response')

    asa kento amier aswer kotha to axios error to amier coda problem ta ke bhi .. 